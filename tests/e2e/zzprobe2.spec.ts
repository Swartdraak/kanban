// NOTE (Remy, 2026-08-29): second debug probe retired — see zzprobe.spec.ts.
import { test } from '@playwright/test';

test.skip('seeded sort probe retired — see kanban.spec.ts Sorting suite', () => {
  // intentionally empty
});
